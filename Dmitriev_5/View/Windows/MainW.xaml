<Window x:Class="Dmitriev_5.View.Windows.MainW"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dmitriev_5.View.Windows"
        mc:Ignorable="d"
        Height="900" Width="1200" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="2" Background="Blue"/>
       
        <TextBlock Name="LoginTbl" Grid.Column="1" Text="{Binding CompanyName}" VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="28" FontWeight="Bold" Padding="5" Foreground="White"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <Border CornerRadius="25" Background="blue" Margin="0 50">
            <Button Content="Заказы" FontSize="36" Foreground="White" Height="100" Background="Transparent" FontWeight="Bold" BorderThickness="0"/>
            </Border>
            
            <Border CornerRadius="25" Background="blue" >
            <Button Content="Профиль" FontSize="32" Foreground="White" Height="80" Background="Transparent" FontWeight="Bold" BorderThickness="0" Click="Button_Click"/>
            </Border>
            
            <Border CornerRadius="25" Background="blue" Margin="0 370 0 0" VerticalAlignment="Bottom">
                <Button Name="QuitBtn" Content="Выйти" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="32" Click="QuitBtn_Click"  HorizontalAlignment="Center" Width="180" Height="80" FontWeight="Bold"/>
            </Border>
        </StackPanel>

        <ListView Grid.Row="1" Grid.Column="1" Name="ListV" FontSize="20" SelectionChanged="ListV_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата заказа" DisplayMemberBinding="{Binding OrderDate, StringFormat=dd.MM.yyyy}"/>
                    <GridViewColumn Header="Стоимость" DisplayMemberBinding="{Binding TotalAmount}"/>
                    <GridViewColumn Header="Статус заказа" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Адресс доставки" DisplayMemberBinding="{Binding DeliveryAddress}"/>
                    <GridViewColumn Header="Примечание" DisplayMemberBinding="{Binding Notes}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
